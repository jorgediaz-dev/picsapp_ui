import 'package:flutter/material.dart';

class TaggedView extends StatelessWidget {
  const TaggedView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tagged Tab'),);
  }
}